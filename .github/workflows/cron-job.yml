name: Scheduled Job

on:
  schedule:
    - cron: "44 23 * * *"   # runs every day at 9:00 UTC

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "This runs on a schedule"
